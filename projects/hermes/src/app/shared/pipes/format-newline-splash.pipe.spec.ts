import { FormatNewlineSplashPipe } from './format-newline-splash.pipe';

describe('FormatNewlineSplashPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNewlineSplashPipe();
    expect(pipe).toBeTruthy();
  });
});
